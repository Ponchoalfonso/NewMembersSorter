<!-- Opciones para elegir el grupo que desea ver !-->
<%= render 'groupSerch' %>
<div>
  <% if user_signed_in? %>
    <%= render 'header' %>
  <% else %>
    <%= render 'headerp' %>
  <% end %>
  <div class="body">
    <aside class="sideBar">
      <div class="subtitle baseBox">
        <span class="icon-book baseBox"></span>
        <div>
          <h2>Acciones</h2>
        </div>
      </div>
      <div class="actions">
        <span class="button baseBox" id="showGroups">
          <span class="icon-profile"></span>
          <h3>Elegir grupo</h3>
        </span>
      </div>
    </aside>
    <section class="content">
      <div>
        <div class="subtitle baseBox">
          <span class="icon-paste baseBox"></span>
          <div>
            <h2>Admisiones</h2>
            <p>Aquí puedes encontrar todos los alumnos aceptados.</p>
          </div>
        </div>
        <div class="info">
          <p>
            Los alumnos que hiceron protocolo para ingresar a la institución fueron admitidos bajo el funcionamiento de un programa, el cual revisa que los
            estudiantes que solicitaron entrar a la institución cumplan con ciertos parámetros predefinidos dentro del código del software.
          </p>
        </div>
      </div>
    </section>
    <%= render 'admited' %>
  </div>
</div>
